# API Overview

The versioned API is served under `/api/v1`. Major resources are `/auth`, `/dashboard`, `/cases`, `/imports`, `/staff`, `/hr`, `/companies`, `/form-templates`, `/document-templates`, `/reports`, `/roles`, `/settings`, `/audit-logs` and `/notifications`. Interactive OpenAPI documentation is available at `/docs` outside restricted production configurations.
