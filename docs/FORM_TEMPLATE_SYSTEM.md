# Form Template System

Templates are database-driven and versioned by company and case type. Sections and ordered fields define labels, type, options, validation, required state, prefill source and document mapping. Case forms snapshot the template version. Bank-supplied values are badged/read-only where configured and retain their originals even after authorized correction.
